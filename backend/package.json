{
  "name": "backend",
  "version": "1.0.0",
  "description": "FastAPI backend for CMS",
  "main": "index.py",
  "scripts": {
    "start": "python -m uvicorn backend.main:app --host 0.0.0.0 --port 5000",
    "dev": "python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 5000",
    "seed": "python -m backend.seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {}
}
